# --- CORE DATA STACK ---
polars>=0.20.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
pyarrow>=14.0.0      # Critical for Parquet file I/O
tabulate>=0.9.0      # For formatted table output

# --- LIVE TRADING & HARVESTING (AWS Compatible) ---
fyers-apiv3>=3.0.0
websocket-client>=1.6.0
# Optional: For headless browser login on AWS
selenium>=4.15.0
webdriver-manager>=4.0.0
pyotp>=2.9.0

# --- SIMULATION & ML (ABIDES Dependencies) ---
# Note: ABIDES itself is installed from source (see guide below)
matplotlib>=3.7.0
scipy>=1.10.0

# --- DEEP LEARNING (Laptop Only) ---
# On AWS/CPU machines, this installs the CPU version.
# On your RTX 3080 Ti, install the CUDA version manually (see guide).
torch>=2.1.0